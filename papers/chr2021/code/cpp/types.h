#ifndef H_TYPES
#define H_TYPES

#include <string>
#include <vector>
#include <numeric>
#include <iostream>
#include <fstream>
#include <sstream>
#include <random>
#include <cassert>
#include <chrono>
#include <algorithm>
#include <map>
#include <unordered_map>
#include <set>

typedef int inttype;
typedef std::vector<std::vector<inttype>>::size_type msztype;
typedef std::vector<inttype>::size_type vsztype;
typedef std::vector<std::vector<float>> matrixf;
typedef std::vector<std::vector<double>> matrixd;
typedef std::vector<std::vector<inttype>> matrixi;
typedef std::vector<std::vector<std::vector<inttype>>> tensor3i;

#endif